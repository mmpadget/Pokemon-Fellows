'use strict';
// Render text and html to the page.

(function(module) {
  const aboutView = {};

  // TODO: Filtering, templates, handlebars, toHtml.
  // NOTE: aboutView.doSomething = function() {};

  // Make aboutView available on global scope.
  module.aboutView = aboutView;
})(window);
